<Window x:Class="MonitorPlatform.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MonitorPlatform.Wpf"
        mc:Ignorable="d"
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        AllowsTransparency="True"
        Background="{x:Null}"
        Name="main"
        Title="测温监控台平台" Height="800" Width="1400">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Assets/Styles/MainViewDictionary.xaml"/>
                <ResourceDictionary Source="../Assets/Styles/DropmenuStyle.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Border BorderBrush="#124968" Background="#05202F" BorderThickness="2" Margin="5" CornerRadius="5">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="1" BlurRadius="10" Opacity="0.5" Direction="0" Color="Gray"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="55"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <!--顶级菜单部分-->
            <Border Style="{StaticResource GlobalBackgroundColor}" BorderThickness="1" MouseLeftButtonDown="Border_MouseLeftButtonDown">
                <StackPanel Orientation="Horizontal">
                    <Border Background="#05202F" Margin="0" MinWidth="240">
                        <TextBlock 
                            Text="&#xe631; 测温在线监控平台" 
                            Style="{StaticResource MainTitle}" 
                            FontFamily="../Assets/Fonts/#iconfont">
                            <TextBlock.Effect>
                                <DropShadowEffect Color="#55D0C5" Direction="0" ShadowDepth="0"/>
                            </TextBlock.Effect>
                        </TextBlock>
                    </Border>
                </StackPanel>
            </Border>
            
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition MaxWidth="240"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <!--左侧菜单-->
                <Border Style="{StaticResource GlobalBackgroundColor}">
                    <StackPanel Margin="0">
                        <!--菜单绑定-->
                        <ItemsControl>
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <VirtualizingStackPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                        <Expander ExpandDirection="Down"  SnapsToDevicePixels="True" VerticalAlignment="Top" Style="{DynamicResource ExpanderStyle}" >
                            <Expander.Header>
                                <StackPanel Orientation="Horizontal" Margin="20,10">
                                    <TextBlock  FontFamily="../Assets/Fonts/#iconfont" Text="&#xe778;" 
                                   Style="{StaticResource  MenuColor}"  VerticalAlignment="Center"></TextBlock>
                                    <TextBlock Text="首页" Margin="10,0,0,0"  Style="{StaticResource  MenuColor}" ></TextBlock>
                                </StackPanel>
                            </Expander.Header>
                            <Expander.Content>
                                <StackPanel Background="#00243B">
                                    <RadioButton Style="{DynamicResource RadioButtonStyle}">控制中心</RadioButton>
                                    <RadioButton Style="{DynamicResource RadioButtonStyle}">资源管理</RadioButton>
                                    <RadioButton Style="{DynamicResource RadioButtonStyle}">仪表菜单</RadioButton>
                                    <RadioButton Style="{DynamicResource RadioButtonStyle}">顶部导航</RadioButton>
                                    <RadioButton Style="{DynamicResource RadioButtonStyle}">通知中心</RadioButton>
                                    <RadioButton Style="{DynamicResource RadioButtonStyle}">蓝牙设置</RadioButton>
                                </StackPanel>
                            </Expander.Content>
                        </Expander>
                        <Expander ExpandDirection="Down"  SnapsToDevicePixels="True" VerticalAlignment="Top" Style="{DynamicResource ExpanderStyle}" >
                            <Expander.Header>
                                <StackPanel Orientation="Horizontal" Margin="20,10">
                                    <TextBlock  FontFamily="../Assets/Fonts/#iconfont" Text="&#xe778;" 
                                   Style="{StaticResource  MenuColor}"  VerticalAlignment="Center"></TextBlock>
                                    <TextBlock Text="首页" Margin="10,0,0,0"  Style="{StaticResource  MenuColor}" ></TextBlock>
                                </StackPanel>
                            </Expander.Header>
                            <Expander.Content>
                                <StackPanel Background="#00243B">
                                    <RadioButton Style="{DynamicResource RadioButtonStyle}">控制中心</RadioButton>
                                    <RadioButton Style="{DynamicResource RadioButtonStyle}">资源管理</RadioButton>
                                    <RadioButton Style="{DynamicResource RadioButtonStyle}">仪表菜单</RadioButton>
                                    <RadioButton Style="{DynamicResource RadioButtonStyle}">顶部导航</RadioButton>
                                    <RadioButton Style="{DynamicResource RadioButtonStyle}">通知中心</RadioButton>
                                    <RadioButton Style="{DynamicResource RadioButtonStyle}">蓝牙设置</RadioButton>
                                </StackPanel>
                            </Expander.Content>
                        </Expander>
                    </StackPanel>
                </Border>
                <!--右侧内容-->
                <StackPanel Grid.Column="1">
                    <Grid Background="#00243B" Height="40" />
                     <!--内容部分-->
                    <ContentControl  Margin="10">
                        
                    </ContentControl>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
